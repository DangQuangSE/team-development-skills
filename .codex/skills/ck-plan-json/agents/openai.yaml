interface:
  display_name: "Plan JSON"
  short_description: "Create phased JSON plan bundles with a compact master and per-phase files"
  default_prompt: "Use ck:plan-json with a spec or inline requirement to create a validated master plan.json and sibling phase JSON files"
