interface:
  display_name: "Plan JSON"
  short_description: "Generate structured JSON plans for AI-driven phased execution"
  default_prompt: "Use /ck:plan-json --spec <path> to create a plan.json with step state tracking, resume, and debug logs"
