---
description: Generate a structured plan.json from a spec for AI-driven phased execution. Flags: --spec <path> (required), --output <path> (default: plans/<slug>/plan.json). Always auto-detects complexity level.
---

Load the `ck:plan-json` skill and run it with `$ARGUMENTS`.
