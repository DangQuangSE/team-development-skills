---
description: Guided feature development with codebase understanding and architecture focus. Modes: --fast (skip test/review), --hard (mandatory reviewers, no auto-approve). Flags: --no-test (skip tester), --tdd (tests-first). Accepts an optional plan file path: python antigravity/run.py cook plans/260418-auth/plan.md
---

Load the `at:cook` skill and run it with `$ARGUMENTS`.
