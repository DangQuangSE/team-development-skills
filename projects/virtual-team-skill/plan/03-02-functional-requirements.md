# Plan: §3.2 Functional Requirements — Virtual Team Skill

**Total FRs: 42**
**Essential: 36 | Conditional: 6 | Optional: 0**

---

## Feature Cluster A — Workflow Engine

### A1: Dual Trigger Mode

| FR-ID | Priority  | Actor              | Shall-Stub                                                                                                                                                                                                                                                                                          | GWT Stub                                                                                                                                                                                                                                                               |
| ----- | --------- | ------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| FR-01 | Essential | ACTOR-01, ACTOR-04 | The system shall provide a `/team "{requirement}" [--project {slug}]` command that executes the complete agent pipeline (BA → TechLead → PM → BE Dev → FE Dev → Tester → QA/QC) automatically from a single operator input without requiring further interaction                                    | Given: operator runs `/team "build a todo app" --project todo-v1` / When: command is executed / Then: all 7 agents run in sequence, all artifacts written, QA sign-off produced, and pipeline summary displayed                                                        |
| FR-02 | Essential | All operators      | The system shall provide individual role commands (`/team-ba`, `/team-techlead`, `/team-pm`, `/team-dev`, `/team-fe`, `/team-test`, `/team-qa`) that invoke each agent independently, allowing operators to control pipeline execution step-by-step                                                 | Given: operator runs `/team-techlead --project todo-v1` / When: command executes / Then: TechLead agent reads BA artifacts from disk and generates architecture files; no other agents are triggered                                                                   |
| FR-03 | Essential | All operators      | The system shall verify that all upstream artifacts (required input files for the requested agent) exist and are non-empty on disk before spawning a per-agent command; if any upstream artifact is missing, the system shall display the specific missing file path and the command to generate it | Given: operator runs `/team-techlead --project todo-v1` but `team/ba/` does not exist / When: command executes / Then: system displays "Missing: projects/todo-v1/team/ba/requirements.md — run: /team-ba --project todo-v1 first" and exits without spawning TechLead |

### A2: Context Chain & Persistence

| FR-ID | Priority  | Actor            | Shall-Stub                                                                                                                                                                                                                                                         | GWT Stub                                                                                                                                                                                                                                                                                              |
| ----- | --------- | ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| FR-04 | Essential | All agent actors | The system shall flush all artifact files produced by an agent to disk (Write tool must return success) before spawning the next agent in the pipeline; an agent must not begin execution while any of its upstream artifacts are still being written              | Given: BA agent has finished generating content / When: pipeline proceeds / Then: all BA artifact files (`requirements.md`, `user-stories.md`, `acceptance-criteria.md`, `business-rules.md`) are confirmed written to disk before TechLead agent is spawned                                          |
| FR-05 | Essential | All operators    | The system shall allow an operator to resume pipeline execution from any phase after a Claude Code session restart, by reading existing artifacts from disk; the system shall not require the operator to re-enter requirement input if BA artifacts already exist | Given: full-auto pipeline completed BA phase and was interrupted at TechLead / When: operator restarts Claude Code and runs `/team-techlead --project {slug}` / Then: TechLead agent reads existing BA artifacts from disk and generates architecture without requiring the original requirement text |
| FR-06 | Essential | All agent actors | Each agent shall read and incorporate the artifacts from all preceding agents in the pipeline as explicit context before generating its own output, according to the defined context dependency map                                                                | Given: FE Dev agent is invoked / When: agent begins generating frontend code / Then: agent reads `team/techlead/tech-stack.md` and `team/be/` (API contracts) before generating any UI code                                                                                                           |

### A3: Hybrid Workflow Enforcement

| FR-ID | Priority  | Actor              | Shall-Stub                                                                                                                                                                                                                           | GWT Stub                                                                                                                                                                                                     |
| ----- | --------- | ------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| FR-07 | Essential | PM Agent, QA Agent | The system shall define and document three milestone gates — Gate 1: Design Freeze (after TechLead), Gate 2: UAT Readiness (after Tester), Gate 3: Release Sign-off (after QA/QC) — and record gate status in the relevant artifacts | Given: TechLead agent completes / When: TechLead writes `architecture.md` / Then: Gate 1 (Design Freeze) status is recorded as PASSED with timestamp in `architecture.md` footer                             |
| FR-08 | Essential | Orchestrator       | In full-auto mode, the system shall continue pipeline execution past milestone gates without pausing, recording each gate as PASSED in the artifact; gates shall not block automation unless a validation hard stop occurs           | Given: full-auto pipeline runs through Gate 1 (TechLead complete) / When: gate is reached / Then: system logs "Gate 1: Design Freeze — PASSED" and immediately spawns PM agent without operator confirmation |

### A4: Multi-project Isolation

| FR-ID | Priority  | Actor         | Shall-Stub                                                                                                                                                                                                                                                                                                              | GWT Stub                                                                                                                                                                                                                        |
| ----- | --------- | ------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| FR-09 | Essential | All operators | The system shall store all artifacts for a project under `projects/{slug}/team/` with no shared files, shared state, or cross-references between different project slugs                                                                                                                                                | Given: two projects exist (`todo-v1` and `ecom-v2`) / When: TechLead agent for `todo-v1` reads artifacts / Then: it reads only from `projects/todo-v1/team/` and has no access to `projects/ecom-v2/`                           |
| FR-10 | Essential | All operators | All per-agent skill commands shall accept an optional `--project {slug}` parameter; when the parameter is omitted, the system shall use a default slug derived from the current working directory name or prompt the operator to provide one [NEEDS USER INPUT: confirm default-slug behavior when `--project` omitted] | Given: operator runs `/team-ba "build a login page"` without `--project` / When: command executes / Then: system uses working directory name as slug, confirms with operator: "Using project slug: {dir-name}. Continue? (y/n)" |

---

## Feature Cluster B — Agent Roles & Artifacts

### B1: BA Phase — Requirement Analysis

| FR-ID | Priority    | Actor    | Shall-Stub                                                                                                                                                                                                            | GWT Stub                                                                                                                                                                                                                                           |
| ----- | ----------- | -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| FR-11 | Essential   | ACTOR-05 | The system shall invoke a BA agent that generates all four required BA artifacts: `requirements.md`, `user-stories.md`, `acceptance-criteria.md`, and `business-rules.md` in the `projects/{slug}/team/ba/` directory | Given: operator provides requirement "build a todo app with auth" / When: BA agent runs / Then: four files are written to `projects/{slug}/team/ba/` within a single agent invocation                                                              |
| FR-12 | Essential   | ACTOR-05 | The BA agent shall format each user story as "As a {actor}, I want {action} so that {benefit}" and assign a unique Story ID in the format `US-{n}` (e.g., US-01, US-02)                                               | Given: BA agent is generating `user-stories.md` / When: a user story is written / Then: every story entry contains the "As a / I want / So that" format and a unique US-{n} ID                                                                     |
| FR-13 | Essential   | ACTOR-05 | The BA agent shall format acceptance criteria using Given/When/Then syntax, with each criterion linked to its parent user story by Story ID                                                                           | Given: user story US-03 exists / When: BA writes `acceptance-criteria.md` / Then: US-03's criteria appear as "Given {precondition} / When {action} / Then {expected result}"                                                                       |
| FR-14 | Conditional | ACTOR-05 | The BA agent shall accept `projects/{slug}/spec.md` and `projects/{slug}/brainstorm.md` as primary or supplemental input in place of free-text requirement, reading those files before generating any BA artifacts    | Given: `projects/todo-v1/spec.md` exists and operator runs `/team-ba --project todo-v1 --srs` / When: BA agent executes / Then: BA reads `spec.md` and generates user stories derived from §3 features and §6 business rules in that spec          |
| FR-15 | Essential   | ACTOR-05 | The BA agent shall document all assumptions made when the requirement input is ambiguous or incomplete, in a clearly labeled `## Assumptions` section within `requirements.md`                                        | Given: operator provides a vague requirement "make a dashboard" / When: BA agent generates `requirements.md` / Then: `## Assumptions` section lists specific assumptions made (e.g., "Assumed: dashboard displays metrics, not a navigation menu") |

### B2: TechLead Phase — Architecture & Design

| FR-ID | Priority  | Actor    | Shall-Stub                                                                                                                                                                                                                            | GWT Stub                                                                                                                                                                                                                      |
| ----- | --------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| FR-16 | Essential | ACTOR-06 | The system shall invoke a TechLead agent that generates all five required TechLead artifacts: `architecture.md`, `tech-stack.md`, `ERD.md`, `sequence-diagrams.md`, and at least one `ADR-001.md` in `projects/{slug}/team/techlead/` | Given: BA artifacts exist / When: TechLead agent runs / Then: all five files are written; `ADR-001.md` covers at minimum the core architectural pattern chosen                                                                |
| FR-17 | Essential | ACTOR-06 | The TechLead agent shall generate all diagrams (architecture overview, ERD, sequence diagrams) using Mermaid syntax embedded in Markdown code fences (`mermaid ... `)                                                                 | Given: TechLead is writing `ERD.md` / When: entity relationship diagram is generated / Then: the diagram appears inside a ```mermaid block and uses valid Mermaid ER diagram syntax                                           |
| FR-18 | Essential | ACTOR-06 | The TechLead agent shall write one ADR file per major architectural decision using the Context / Decision / Consequences format, numbered sequentially (`ADR-001.md`, `ADR-002.md`, ...)                                              | Given: TechLead decides to use PostgreSQL over MongoDB / When: ADR is written / Then: `ADR-001.md` contains ## Context (why a decision was needed), ## Decision (what was chosen), ## Consequences (trade-offs, implications) |

### B3: PM Phase — Sprint Planning

| FR-ID | Priority    | Actor    | Shall-Stub                                                                                                                                                                                                                                       | GWT Stub                                                                                                                                                                                             |
| ----- | ----------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| FR-19 | Essential   | ACTOR-07 | The system shall invoke a PM agent that generates three required PM artifacts: `sprint-plan.md`, `task-breakdown.md`, and `story-points.md` in `projects/{slug}/team/pm/`                                                                        | Given: BA and TechLead artifacts exist / When: PM agent runs / Then: three files are written with organized sprints, task list, and story point estimates                                            |
| FR-20 | Conditional | ACTOR-07 | The PM agent shall use the Claude Code TodoWrite tool to create a task entry for each task in the sprint plan, with `status: "pending"`, and shall update task status to `"in_progress"` / `"completed"` as agents progress through the pipeline | Given: PM agent generates 12 tasks in the sprint plan / When: PM agent writes sprint plan / Then: 12 TodoWrite entries are created with status "pending"                                             |
| FR-21 | Essential   | ACTOR-07 | The PM agent shall organize user stories into two-week sprints, assign story points (S=1, M=3, L=5, XL=8) to each task, and define sprint goals for each sprint                                                                                  | Given: there are 8 user stories / When: PM generates `sprint-plan.md` / Then: stories are grouped into Sprint 1 and Sprint 2 (or more), each sprint has a goal statement and total story point count |

### B4: BE Dev Phase — Backend Code Generation

| FR-ID | Priority  | Actor    | Shall-Stub                                                                                                                                                                                                                                                           | GWT Stub                                                                                                                                                                                                                 |
| ----- | --------- | -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| FR-22 | Essential | ACTOR-08 | The system shall invoke a BE Dev agent that generates backend source code files (API routes/controllers, database schema/ORM models, migration files, service/business logic files) and a `pr-description.md` in `projects/{slug}/team/be/`                          | Given: TechLead and PM artifacts exist / When: BE Dev agent runs / Then: code files appropriate to the selected tech stack are written under `team/be/` along with `pr-description.md`                                   |
| FR-23 | Essential | ACTOR-08 | The BE Dev agent shall read `tech-stack.md` before generating any code and shall generate code using the language, framework, and patterns specified in that file                                                                                                    | Given: `tech-stack.md` specifies "Backend: Node.js with Express" / When: BE Dev generates API routes / Then: all generated code uses Express.js syntax and Node.js conventions                                           |
| FR-24 | Essential | ACTOR-08 | The BE Dev agent shall not hardcode any credentials, API keys, passwords, tokens, or connection strings in generated code; all such values shall be replaced with environment variable references (e.g., `process.env.DB_PASSWORD`) and documented in `.env.example` | Given: BE Dev generates a database connection module / When: connection string is needed / Then: code contains `process.env.DATABASE_URL` (or equivalent) and `.env.example` lists `DATABASE_URL=your_database_url_here` |

### B5: FE Dev Phase — Frontend Code Generation

| FR-ID | Priority  | Actor    | Shall-Stub                                                                                                                                                                                                                                                       | GWT Stub                                                                                                                                                                                                                               |
| ----- | --------- | -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| FR-25 | Essential | ACTOR-09 | The system shall invoke a FE Dev agent that generates frontend source code files (UI component files, page/view files, API integration service files, state management files) and a `pr-description.md` in `projects/{slug}/team/fe/`                            | Given: TechLead and BE Dev artifacts exist / When: FE Dev agent runs / Then: frontend code files appropriate to the selected tech stack are written under `team/fe/`                                                                   |
| FR-26 | Essential | ACTOR-09 | The FE Dev agent shall read BE Dev artifacts (specifically API route definitions and response schemas) before generating API integration code, ensuring alignment between frontend API calls and backend route signatures                                        | Given: BE Dev generated `routes/auth.js` with `POST /api/auth/login` returning `{ token, user }` / When: FE Dev generates API integration / Then: frontend service calls `POST /api/auth/login` and handles `{ token, user }` response |
| FR-27 | Essential | ACTOR-09 | The FE Dev agent shall not hardcode environment-specific values (API base URLs, feature flags, configuration) in generated source code; all such values shall use a configuration or environment variable pattern appropriate to the selected frontend framework | Given: FE Dev generates an API service / When: API base URL is needed / Then: code reads from `process.env.REACT_APP_API_URL` (React) or `import.meta.env.VITE_API_URL` (Vite) or framework equivalent                                 |

### B6: Tester Phase — Test Plan & Test Cases

| FR-ID | Priority  | Actor    | Shall-Stub                                                                                                                                                                                                                                   | GWT Stub                                                                                                                                                                                                                                                 |
| ----- | --------- | -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| FR-28 | Essential | ACTOR-10 | The system shall invoke a Tester agent that generates five required Tester artifacts: `test-plan.md`, `test-cases-unit.md`, `test-cases-integration.md`, `test-cases-e2e.md`, and `bug-report-template.md` in `projects/{slug}/team/tester/` | Given: BA, BE Dev, and FE Dev artifacts exist / When: Tester agent runs / Then: all five files are written to `team/tester/`                                                                                                                             |
| FR-29 | Essential | ACTOR-10 | The Tester agent shall derive test cases from BA acceptance criteria, mapping each Given/When/Then acceptance criterion to at least one corresponding test case with test ID, scenario name, steps, input data, and expected output          | Given: BA wrote "Given user is unauthenticated / When accessing /dashboard / Then redirect to /login" / When: Tester generates e2e test cases / Then: a test case TC-E2E-{n} covers this scenario with specific steps and expected redirect behavior     |
| FR-30 | Essential | ACTOR-10 | The Tester agent shall flag any detected logic inconsistencies between BA requirements/acceptance criteria and the BE/FE implementation artifacts in a `## Flags from Previous Agents` section                                               | Given: BA requires "password must be 8+ chars" but BE code has no length validation / When: Tester reads both artifacts / Then: Tester writes FLAG-TESTER-01 in `## Flags from Previous Agents`: severity Major, describes missing validation in BE code |

### B7: QA/QC Phase — Quality Gate & Sign-off

| FR-ID | Priority  | Actor    | Shall-Stub                                                                                                                                                                                                                                                                                                                                                                                     | GWT Stub                                                                                                                                                                                                                                   |
| ----- | --------- | -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| FR-31 | Essential | ACTOR-11 | The system shall invoke a QA/QC agent that generates three required QA artifacts: `quality-report.md`, `compliance-check.md`, and `sign-off.md` in `projects/{slug}/team/qa/` after reading all artifacts from all preceding agents                                                                                                                                                            | Given: all preceding agents have completed and artifacts exist / When: QA/QC agent runs / Then: three files are written to `team/qa/`                                                                                                      |
| FR-32 | Essential | ACTOR-11 | The QA/QC agent shall perform four checks: (1) completeness check — all required sections present in each artifact; (2) cross-artifact consistency — requirements, architecture, code, and tests are mutually consistent; (3) security artifact review — no hardcoded credentials in code artifacts; (4) process compliance — all milestone gates passed, all ADRs present for major decisions | Given: BE Dev code contains a hardcoded API key string / When: QA/QC reviews `team/be/` files / Then: `quality-report.md` records a Critical finding: "Hardcoded credential detected in {filename}" with file reference and line indicator |
| FR-33 | Essential | ACTOR-11 | The QA/QC `sign-off.md` shall contain a verdict of exactly one of: APPROVED (no Critical or Major issues), CONDITIONAL (Minor issues present; list of conditions), or REJECTED (Critical or Major unresolved issues; must be rerun) plus a dated entry and list of findings                                                                                                                    | Given: QA/QC finds 2 Minor issues and 0 Critical/Major issues / When: `sign-off.md` is written / Then: verdict is "CONDITIONAL" with 2 listed conditions and date stamp                                                                    |

---

## Feature Cluster C — Error Handling & Validation

### C1: Automated Validation Layer (Layer 1)

| FR-ID | Priority  | Actor        | Shall-Stub                                                                                                                                                                                                                                                                                  | GWT Stub                                                                                                                                                                                                                                                                            |
| ----- | --------- | ------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| FR-34 | Essential | Orchestrator | The system shall validate each agent's artifact set for structural completeness immediately after artifacts are written to disk, by checking that all required section headings (defined in the agent's validation schema) are present in each file                                         | Given: BA agent writes `user-stories.md` / When: validation runs / Then: system checks for required headings: `## User Stories`, `## Story ID Index`; if both present → pass; if any missing → fail                                                                                 |
| FR-35 | Essential | Orchestrator | The system shall automatically rerun a failed agent with an augmented prompt listing the missing sections when structural validation fails; the system shall retry up to a maximum of 3 times; if validation fails on the 3rd attempt, the system shall issue a hard stop                   | Given: BA validation fails (missing `## Business Rules` section) / When: retry 1 runs / Then: BA prompt includes "RETRY REQUIRED — missing sections: [## Business Rules] — please ensure all required sections are present" / Given: retry 3 also fails / Then: hard stop triggered |
| FR-36 | Essential | Orchestrator | The system shall write a validation failure log to `projects/{slug}/validation-errors/{agent}-attempt-{n}.md` for every failed validation attempt, recording: timestamp (ISO 8601), agent name, attempt number, list of sections found, list of sections missing, and raw validation result | Given: BA validation fails on attempt 2 / When: failure is recorded / Then: file `validation-errors/ba-attempt-2.md` is created with timestamp, agent name "BA", attempt 2, found sections list, missing sections list                                                              |

### C2: Cross-agent Verification Layer (Layer 2)

| FR-ID | Priority  | Actor                        | Shall-Stub                                                                                                                                                                                                                  | GWT Stub                                                                                                                                                                                                                                                                                                                                     |
| ----- | --------- | ---------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| FR-37 | Essential | ACTOR-06, ACTOR-10, ACTOR-11 | Every agent shall include a `## Flags from Previous Agents` section in its primary artifact (or in a dedicated `flags.md` file); if no flags are detected, this section shall contain the text "No flags detected"          | Given: TechLead agent reads BA requirements / When: no inconsistencies found / Then: `architecture.md` contains `## Flags from Previous Agents` with content "No flags detected"                                                                                                                                                             |
| FR-38 | Essential | ACTOR-06, ACTOR-10, ACTOR-11 | Each cross-agent flag shall contain: Flag ID (format: `FLAG-{ROLE}-{n}`), a description of the detected issue, the name of the affected artifact file, severity level (Blocker / Major / Minor), and a suggested resolution | Given: TechLead detects BA user story US-05 is internally contradictory / When: TechLead writes flag / Then: `FLAG-TECHLEAD-01: Description: US-05 states users can be both admin and guest simultaneously — logically contradictory. Affected: team/ba/user-stories.md. Severity: Major. Suggestion: Clarify role hierarchy with operator.` |
| FR-39 | Essential | Orchestrator                 | When any agent produces flags in full-auto mode, the system shall append the flag entries to a `projects/{slug}/flags-summary.md` file and display a summary notification to the operator at pipeline completion            | Given: TechLead and Tester both produce flags / When: full-auto pipeline completes / Then: `flags-summary.md` lists all flags from all agents; CLI displays "⚠️ 3 flags detected across 2 agents — see projects/{slug}/flags-summary.md"                                                                                                     |

### C3: SRS Workflow Integration

| FR-ID | Priority    | Actor    | Shall-Stub                                                                                                                                                                                                                                                                                                                    | GWT Stub                                                                                                                                                                                                                                                               |
| ----- | ----------- | -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| FR-40 | Conditional | ACTOR-05 | The system shall accept `--srs` flag in the `/team-ba` command, causing the BA agent to read `projects/{slug}/spec.md` (and `brainstorm.md` if present) as its primary input instead of free-text requirement                                                                                                                 | Given: `/team-ba --project todo-v1 --srs` is run and `projects/todo-v1/spec.md` exists / When: BA agent executes / Then: BA reads spec.md §3 (Features) and §6 (Business Rules) and derives user stories from them                                                     |
| FR-41 | Conditional | ACTOR-05 | The BA agent shall detect and flag conflicts between SRS artifact content and any operator-provided runtime input, writing the conflict to `## Conflicts Detected` in `requirements.md` and awaiting operator direction before proceeding in per-agent mode [NEEDS USER INPUT: define behavior in full-auto mode — see OI-07] | Given: `spec.md` defines feature X as out-of-scope, but operator runtime input requests feature X / When: BA agent processes both inputs / Then: `## Conflicts Detected` in `requirements.md` lists the conflict; in per-agent mode, BA pauses for operator resolution |

### C4: TodoWrite Progress Tracking

| FR-ID | Priority    | Actor    | Shall-Stub                                                                                                                                                                                                                                                             | GWT Stub                                                                                                                                                                                                                                                                         |
| ----- | ----------- | -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| FR-42 | Conditional | ACTOR-07 | The PM agent shall create one TodoWrite entry per sprint task using `status: "pending"`, and the orchestrator shall update each task's status to `"in_progress"` when its corresponding agent is spawned and `"completed"` when that agent's artifacts pass validation | Given: PM creates tasks: [task-be-api, task-fe-login, task-fe-dashboard] / When: BE Dev agent is spawned / Then: TodoWrite entry for `task-be-api` is updated to `status: "in_progress"` / When: BE Dev validation passes / Then: `task-be-api` updated to `status: "completed"` |

---

## FR Count Summary

| Priority    | Count  | FR IDs                                                                                                                                                                                                                                                            |
| ----------- | ------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Essential   | 36     | FR-01, FR-02, FR-03, FR-04, FR-05, FR-06, FR-07, FR-08, FR-09, FR-10, FR-11, FR-12, FR-13, FR-15, FR-16, FR-17, FR-18, FR-19, FR-21, FR-22, FR-23, FR-24, FR-25, FR-26, FR-27, FR-28, FR-29, FR-30, FR-31, FR-32, FR-33, FR-34, FR-35, FR-36, FR-37, FR-38, FR-39 |
| Conditional | 6      | FR-14, FR-20, FR-40, FR-41, FR-42 — plus FR-10 (partial, default-slug behavior TBD)                                                                                                                                                                               |
| Optional    | 0      | —                                                                                                                                                                                                                                                                 |
| **Total**   | **42** | FR-01 → FR-42                                                                                                                                                                                                                                                     |

---

## Actor Coverage Check

| Actor                       | FRs Covered                                                                 |
| --------------------------- | --------------------------------------------------------------------------- |
| ACTOR-01 (Solo Developer)   | FR-01, FR-02, FR-03, FR-04, FR-05, FR-09, FR-10, FR-22, FR-25, FR-28, FR-31 |
| ACTOR-02 (Technical Lead)   | FR-02, FR-05, FR-16, FR-17, FR-18                                           |
| ACTOR-03 (PM/BA Human)      | FR-02, FR-19, FR-20, FR-21                                                  |
| ACTOR-04 (Startup Founder)  | FR-01, FR-04, FR-09                                                         |
| ACTOR-05 (BA Agent)         | FR-11, FR-12, FR-13, FR-14, FR-15, FR-40, FR-41                             |
| ACTOR-06 (TechLead Agent)   | FR-16, FR-17, FR-18, FR-37, FR-38                                           |
| ACTOR-07 (PM Agent)         | FR-19, FR-20, FR-21, FR-42                                                  |
| ACTOR-08 (BE Dev Agent)     | FR-22, FR-23, FR-24                                                         |
| ACTOR-09 (FE Dev Agent)     | FR-25, FR-26, FR-27                                                         |
| ACTOR-10 (Tester Agent)     | FR-28, FR-29, FR-30, FR-37, FR-38                                           |
| ACTOR-11 (QA/QC Agent)      | FR-31, FR-32, FR-33, FR-37, FR-38                                           |
| Orchestrator (skill system) | FR-03, FR-04, FR-07, FR-08, FR-34, FR-35, FR-36, FR-39                      |

All 11 actors (+ orchestrator) covered. ✓
