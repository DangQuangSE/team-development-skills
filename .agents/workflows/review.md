`/review` — Request code review

1. Load the `code-review` skill from `.agents/skills/code-review/SKILL.md`
2. Scan recent changes (git diff or specified files)
3. Run the full review pipeline
4. Output APPROVED / WARNING / BLOCK with explanation
